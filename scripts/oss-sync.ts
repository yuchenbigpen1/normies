/**
 * OSS sync placeholder.
 *
 * This repo currently doesn't define an automated OSS sync workflow.
 * Keep the script entrypoint valid for CI/dev tooling.
 */

console.log('[oss:sync] No automated OSS sync configured in this fork.');

