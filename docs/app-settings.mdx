# App Settings

App Settings are global defaults for your installation.

Common examples:
- Default model/provider
- Feature toggles
- Environment-level behavior

These settings apply across workspaces unless a workspace-level override exists.
