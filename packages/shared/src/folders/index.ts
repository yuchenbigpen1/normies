/**
 * Folders Module
 *
 * Configurable session folders for workspaces.
 * Folders are exclusive containers (one-per-session), unlike labels which are additive tags.
 *
 * This barrel is browser-safe (no Node.js dependencies).
 * For filesystem operations, import from '@normies/shared/folders/storage'.
 */

// Types
export * from './types.ts';
